# ==============================================================================
# Archivo: python_env.yaml
# ==============================================================================
# Lista de dependencias para ser instaladas con pip en un entorno virtual.
python: "3.9"
dependencies:
  - mlflow
  - boto3
  - transformers
  - datasets
  - torch --index-url https://download.pytorch.org/whl/cpu
  - scikit-learn
  - psutil
  - matplotlib
  - seaborn
  - pandas
